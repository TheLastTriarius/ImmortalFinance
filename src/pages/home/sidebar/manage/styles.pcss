.root {
  display: flex;
  flex-direction: column;
  list-style: none;
  flex: 1;
  margin: 0;
  padding: 0;
}

.loaderRoot {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: calc(100vh - 10.0625rem);
}
